@{
    //Layout w zależności od tego czy użytkownik jest zalogowany
    Layout = Context.Session.GetString("UserId") == null
        ? "~/Pages/Shared/_Layout.cshtml"
        : "~/Pages/Shared/ClientLayout.cshtml";
}
